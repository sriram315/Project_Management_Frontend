# .github/workflows/deploy.yml
name: Deploy Project Management Frontend

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Upload project to VM
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT || 22 }}
          source: "."
          target: "~/app-pm-frontend"
          overwrite: true
          rm: true

      - name: Build & run container on VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT || 22 }}
          envs: GITHUB_SHA
          script: |
            set -e
            cd ~/app-pm-frontend

            echo "🛑 Stopping any running container..."
            docker stop pm-frontend || true
            docker rm pm-frontend || true

            echo "⚙️  Disabling Docker BuildKit to avoid buildx errors..."
            export DOCKER_BUILDKIT=0
            echo "✅ BuildKit disabled. Proceeding with build..."

            echo "🏗️  Building Docker image..."
            docker build -t pm-frontend:${GITHUB_SHA} .

            echo "🚀 Running new container..."
            docker run -d --name pm-frontend \
              --restart unless-stopped \
              -p 3005:80 \
              pm-frontend:${GITHUB_SHA}

            echo "🧹 Cleaning up unused images..."
            docker image prune -f

            echo "✅ Deployment completed successfully!"
